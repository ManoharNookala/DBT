version: 2

sources:
  - name: staging  # ✅ Refers to the staging dataset
    database: "{{ env_var('GCP_PROJECT_ID') }}"
    schema: "{{ env_var('STAGING_DATASET_NAME') }}"  # ✅ Defined in GitHub Secrets
    tables:
      - name: stg_Iris  # ✅ Table in staging dataset

